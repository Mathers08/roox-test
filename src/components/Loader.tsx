import React from 'react';
// @ts-ignore
import loader from '../assets/loader.gif';

const Loader = () => {
  return (
    <section>
      <img src={loader} alt=""/>
    </section>
  );
};

export default Loader;